package pl.fus;

//import com.google.gson.Gson;
//import com.google.gson.reflect.TypeToken;


public class UrlInitializatior<T> {
//    private static final ObjectMapper objectMapper = new ObjectMapper();
//    public static <T> T[] initObject(URL url) {
//        try (InputStreamReader reader = new InputStreamReader(url.openStream())) {
////            Type collectionType = new TypeToken<Collection<T>>(){}.getType();
////            return new Gson().fromJson(reader, collectionType);
//            T[] value = objectMapper.readValue(url, new TypeReference<T[]>(){});
//            return (T[]) value;
//
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
//    }

//    public Map<K, T> getMapped (URL url){
//        ArrayList<T> arrayList = initObject(url);
//        Map<K, T> map = new HashMap<>();
//        for (T t : arrayList)
//            map.put(t.getId(), u);
//    }
}
